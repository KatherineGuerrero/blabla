<%= form_with(model: playlist_song, local: true) do |form| %>
  <% if playlist_song.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(playlist_song.errors.count, "error") %> prohibited this playlist_song from being saved:</h2>

      <ul>
      <% playlist_song.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <p>Song: <%= @song.name %>, select one of your playlists to add to:</p>
  <div class="actions">
    <%= form.hidden_field :song_id, :value => @song.id %>
    <%= form.select :playlist_id, options_for_select(@user.playlists.collect{ |u| [u.name, u.id] }) %>
    <%= form.submit %>
  </div>
<% end %>
